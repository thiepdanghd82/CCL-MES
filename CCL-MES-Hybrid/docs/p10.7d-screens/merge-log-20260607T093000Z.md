# P10.7d — merge log (stack closeout)

**Tag**: `v0.10.7d`
**Tag commit**: `7a156a8` (main HEAD at close)
**Tag time (UTC)**: 2026-06-07T09:30:00Z
**Closing message** (full text): see `git tag -n100 v0.10.7d`.

## 4-PR stack

| PR  | Code    | Branch                                    | SHA at merge | Merged   | Mid-stack/end | --delete-branch on merge |
| --- | ------- | ----------------------------------------- | ------------ | -------- | ------------- | ------------------------ |
| #117 | 7d-1   | feat/p10.7d-1-ipqc-domain                 | `8d94c9f`    | 2026-06-07 | mid           | NO (Rule 2)              |
| #118 | 7d-2   | feat/p10.7d-2-ipqc-wire                   | `fb2a9d6`    | 2026-06-07 | mid           | NO (Rule 2)              |
| #119 | 7d-3   | feat/p10.7d-3-ipqc-ui                     | `7de963c`    | 2026-06-07 | mid           | NO (Rule 2)              |
| #120 | 7d-4   | feat/p10.7d-4-closeout                    | `7a156a8`    | 2026-06-07T09:30Z | end | YES (end of stack)       |

## Branches deleted in §5 sweep (post-tag)

| Branch                          | Last SHA before delete | When                    |
| ------------------------------- | ---------------------- | ----------------------- |
| feat/p10.7d-1-ipqc-domain       | `94d1635`              | 2026-06-07 post-tag     |
| feat/p10.7d-2-ipqc-wire         | `819f3f2`              | 2026-06-07 post-tag     |
| feat/p10.7d-3-ipqc-ui           | `2ff6aaa`              | 2026-06-07 post-tag     |
| feat/p10.7d-4-closeout          | `7a156a8`              | merge time (--delete-branch) |

Safety SHAs above can be restored via `git fetch origin '+refs/heads/feat/p10.7d-*:refs/heads/feat/p10.7d-*'` (only if the remote ref still exists; otherwise create local from SHA via `git checkout -b <name> <sha>` while the SHA is reachable from the dangling-commit GC window — typically 90 days).

## Test belt at tag

| Suite          | Count | Delta vs v0.10.7c (`b5a5cf2` baseline → `7a156a8`) |
| -------------- | ----- | --------------------------------------------------- |
| Domain         | 811   | +60 (was 751 pre-7d; 14 unit rollup + 12 dual-sig parse + 18 service slot/judgment/dual-sig + 6 LegacyParity + 4 integration + 6 misc) |
| Api            | 359   | +31 (was 328; 30 IpqcReviewController fixtures + 1 7d-4 Drawer L19-amendment fixture) |
| Hybrid.Client  | 575   | +26 (Localiser locked VN bank) |
| Hybrid.Razor   | 99    | +40 (12 IpqcDashboard + 10 QaApprovalDashboard + 2 WorkOrders dispatch + 2 RunningDashboard guard + 14 L21 OnPhaseChanged) |
| Soak           | 4     | unchanged (Concurrent_run_qty_add) |
| Verify         | 16/16 | full closed-out form + reproducible |
| Checkpoint     | 14/14 | checkpoint-7d-final.sh on hardware DB (3 judgment paths + Q3 dual-sig 2-path + audit wire-mirror 4/4 + L21 wire + idempotency) |

## Lessons codified in this stack

| Lesson | Title                                                                                          | Triggered by |
| ------ | ---------------------------------------------------------------------------------------------- | ------------ |
| L20    | Security-critical default-ON flags MUST parse typo-safe + emit boot-probe log line               | 7d-1 design call — Q3 dual-sig flag |
| L21    | Phase-changing actions MUST auto re-fetch summary + re-dispatch                                  | Henry RCA on PR #119 hardware verify |
| L19 amendment | Every WO-returning endpoint MUST project canonical MesPhase (not just /summary)            | Henry RCA on PR #120 step 13 (drawer DTO drift) |

## Bonus fixes shipped in 7d-4

- **DB-path default fix** ([Program.cs:36-90](../../src/CCL.MES.Api/Program.cs)): walk up from `ContentRootPath` looking for an EXISTING `<ancestor>/data/ccl_mes.db` BEFORE falling back to the innermost `.sln` directory. Closes the footgun where `dotnet run` from `CCL-MES-Hybrid/src/CCL.MES.Api/` used to land at the EMPTY `CCL-MES-Hybrid/data/` instead of the real DB at `CCL-MES/data/`.
- **L10 drift-guard helper** (lands in 7d-2 checkpoint, codified): `api_post_admin` / `api_assert_routed` bash helpers bail with `[L10 drift]` banner on HTTP 404/405 so a wrong-endpoint typo triggers a single failure at the source line, not 4 cascaded downstream FAILs.

## Operator-driven cleanup (post-tag)

Run `bash scripts/purge-test-audit.sh` for the dry-run preview, then `--commit` when ready. As of tag time:

```
Pre-purge counts (operator's dev DB):
  TOTAL AuditLogs                                    : 532
  TEST_RESET candidates                              : 5
  WO_SETTING/RUN_* test rows (7c-* / l19-walk)       : 6
  WO_IPQC_*/WO_QA_* test rows (7d-* checkpoints)     : 79
  ManufacturingStructures BOM seed rows              : 5
  Seeded IPQC/QA checkpoint test users               : 2
  TOTAL ALL                                          : 97
```

Real ops audit rows are unaffected — every pattern targets the test-script vocabulary (actor tag, username, Detail substring).

## Next

Open `feat/p10.7e-fqc-domain` against the Q1..Q8 approved scope per `docs/p10.7e-scope-proposal.md`. Stack 7e ships FQC + OQC + Reports + SHIPPED terminal state. First commit lands the contract gaps (SHIPPED enum + WoQcChecks/WoQcCheckItems data-driven schema + WoQcSigPolicyOptions L20-pattern × 3 flags + Product.QcProfileOverride + 12 audit codes + 2 readiness rollup helpers + photo wiring + migration + P10.7-BACKLOG.md OOS entries).
