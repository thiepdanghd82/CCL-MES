# P10.7b merge log — 2026-06-06T16:00:00Z

Stack: PR #107 → #108 → #109 → #111 merged to `main`, tag **`v0.10.7b`** pushed.

Cumulative main HEAD post-merge: **`96e0f41`**

## Stack summary

| PR | SHA | Title |
| --- | --- | --- |
| #107 | `8a201d6` | feat(p10.7b-1): PREPRESS row-level domain + migration + housekeeping |
| #108 | `ae248fc` | feat(p10.7b-2): PREPRESS API endpoints + checkpoint script |
| #109 (feat) | `9e994a3` | feat(p10.7b-3): PREPRESS row-checks Razor + Catalyst UI |
| #109 (fix) | `747cc95` | fix(p10.7b-3): PREPRESS NG-path picker — closes L17 seed + UI gaps |
| #111 (chore) | `809e3b1` | chore(p10.7b-4): test belt closeout — verify + checkpoint + purge + 7c scope |
| #111 (fix) | `96e0f41` | fix(p10.7b-4): honor `--urls` override + assert-bound-port + preserve TMP_DIR |

Plus the canonical docs PR #110 (b42c80c) `docs: canonical LESSONS-LEARNED + SKILLS playbook + CLAUDE.md pre-flight` that landed between 7b-3 and 7b-4.

## Verify on cumulative main (post all 4 merges + L18 fix)

`bash scripts/verify-p10.7b.sh` from main HEAD `96e0f41` — **32 PASS / 0 FAIL**:

```
============================  SUMMARY  ============================
  PASS  solution build (exit 0)
  PASS  Domain tests (passed=733)
  PASS  Api tests (passed=301)
  PASS  Hybrid Client tests (passed=549)
  PASS  Hybrid Razor tests (passed=24)
  PASS  pre-migration baseline (3 PREPRESS tables absent)
  PASS  post-migration (3 PREPRESS tables present)
  PASS  migration re-apply (idempotent NOOP)
  PASS  API bound on 5101 (pid=90367; our dotnet=90357)
  PASS  L18 guard — no 'Overriding address(es)' warning in API log
  PASS  L17 boot probe scrap=8 (≥8)
  PASS  reason-codes anon → 401
  PASS  login admin (token_len=589)
  PASS  reason-codes kind=Scrap auth → 200 + 8 codes (≥8)
  PASS  reason-codes bad kind → 422
  PASS  reason-codes no filter → 3 kinds present (Pause+Scrap+Recovery)
  PASS  GET /prepress → ETag set + 5 materials
  PASS  PUT /materials no If-Match → 428
  PASS  PUT /materials no Idempotency-Key → 400
  PASS  PUT /materials bad If-Match → 409 wo.state_conflict + fresh ETag
  PASS  PUT /materials Ok happy → 200 + bumped ETag
  PASS  PUT /materials Ng no reason → 422
  PASS  PUT /materials Ng unregistered code → 422
  PASS  PUT /materials Ng with SC-MAT-DAMAGE → 200
  PASS  PUT /plate-check Ok → 200
  PASS  PUT /cutter-check Ok → 200
  PASS  rollup MaterialsReady=false (1 row NG ⇒ not ready)
  PASS  audit wire WO_PREPRESS_MATERIAL_SET visible for WO 5
  PASS  audit wire WO_PREPRESS_PLATE_SET visible for WO 5
  PASS  audit wire WO_PREPRESS_CUTTER_SET visible for WO 5
  PASS  audit wire WO_STATE_CONFLICT visible for WO 5
  PASS  Concurrent_prepress_row_updates_N_equals_10 soak

  TOTAL: pass=32 fail=0
```

## Feature branches deleted (post-merge sweep, R2 compliance)

```
$ git push origin --delete feat/p10.7b-1-prepress-domain     → deleted
$ git push origin --delete feat/p10.7b-2-prepress-endpoints  → deleted
$ git push origin --delete feat/p10.7b-3-prepress-ui         → deleted
$ git push origin --delete feat/p10.7b-4-test-belt-closeout  → deleted
$ git branch -r | grep "p10.7b" → (none)
```

## Tag v0.10.7b

```
$ git tag -a v0.10.7b -m "P10.7b — PREPRESS row checks (domain + API + Razor UI + L17 picker + test belt)"
$ git push origin v0.10.7b → new tag
$ git tag --list "v0.10*"
v0.10.6
v0.10.7a-1
v0.10.7a-2
v0.10.7b
```

## Lessons added during 7b stack

- **L17** — Seed early-exit guards skip kind-specific data (DbSeeder global `AnyAsync()` short-circuit blocked Pause + Scrap when Recovery already seeded). Surfaced in 7b-3 Catalyst NG-path test; codified via 5 bUnit + 1 wire-mirror integration test + boot probe `[seed] reason_codes pause=N scrap=M recovery=K`.
- **L18** — `--urls` / `ASPNETCORE_URLS` overridden by hardcoded `Kestrel:Endpoints` in `appsettings.json` (API stuck on port 5100). Surfaced in 7b-4 verify-script first run; codified via lsof-bound-port assertion + log-grep regression guard + SKILLS.md S10 (preserve TMP_DIR on FAIL) + S11 (assert-bound-port pattern).

## Skills added during 7b stack

- **S10** — Preserve debug artifacts on FAIL (don't auto-clean `$TMP_DIR` before operator can read it). Closes the meta-RCA gap surfaced by L18: original verify script `rm -rf $TMP_DIR` unconditionally on exit, deleting the api.log Henry needed to prove the Kestrel override warning.
- **S11** — Assert-bound-port on every script that boots an API (lsof + log grep for "Overriding address"). Pattern includes pre-boot kill of stale listeners, explicit `--urls` flag, log-grep ready signal, lsof verification.

## Exit criteria

- ✅ All 4 PRs merged to main without cascade-close (R2 deferred branch cleanup respected).
- ✅ Tag `v0.10.7b` pushed.
- ✅ verify-p10.7b.sh 32/32 PASS on main.
- ✅ 4 feature branches deleted from origin + local.
- ✅ L17 + L18 codified with test guards (no prose lessons).
- ✅ L17 boot probe + L18 lsof + log-grep guards mandatory in 7c verify-script onward.
- ✅ purge-test-audit.sh extended to remove 7b test artefacts (dry-run preview clean; Henry runs `--commit` to actually delete the 5 BOM seed rows + 5 TEST_RESET audit rows).
- 🟡 7c scope proposal **enriched** version requested by Henry (per-Q recommendation + SpecHub citation + trade-off + contract gap). Filed at `docs/p10.7c-scope-proposal.md` (replaces the bare Q1-Q6 list).

**P10.7b — DONE.**
