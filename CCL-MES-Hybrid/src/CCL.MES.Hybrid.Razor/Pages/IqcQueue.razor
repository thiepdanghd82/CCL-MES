@page "/qms/iqc"
@attribute [Microsoft.AspNetCore.Authorization.Authorize]
@inherits CCL.MES.Hybrid.Razor.Localization.LocalizedComponentBase

@* IQC — incoming raw-material inspection. Data is API-backed (/api/v2/iqc,
   QcRead) but the in-app worklist UI is not wired yet, so this is an honest
   scaffold rather than a dead link. See TranslationCatalog.Qms qms.iqc.*. *@
<PageTitle>@T("qms.iqc.pagetitle") — CCL MES</PageTitle>

<QmsPlaceholder Title="@T("qms.iqc.title")"
                Subtitle="@T("qms.iqc.subtitle")"
                Heading="@T("qms.iqc.heading")"
                Description="@T("qms.iqc.desc")" />
