@page "/qcqa/oqc"
@using CCL.MES.Domain
@inject IStringLocalizer<SharedResource> Loc

@*  Phase 6 Bước 3 — OQC (Outgoing Quality Control).
    Reuses <QcInspectionGrid /> with Type=QcType.OQC. Same shape as
    Ipqc.razor — column labels + status badges shared via the component
    + qcqa.grid.* i18n keys. *@

<QcInspectionGrid Type="QcType.OQC" Title="@Loc["qcqa.oqc.title"].Value" />
