@page "/qcqa/ipqc"
@using CCL.MES.Domain
@inject IStringLocalizer<SharedResource> Loc

@*  Phase 6 Bước 3 — IPQC (In-Process Quality Control).
    Reuses <QcInspectionGrid /> with Type=QcType.IPQC. Read-only this Bước;
    Create / Approve already exposed by QcService + driven by the
    Work Orders page (Phase 0). *@

<QcInspectionGrid Type="QcType.IPQC" Title="@Loc["qcqa.ipqc.title"].Value" />
